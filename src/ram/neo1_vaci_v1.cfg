# Link VACI at its WozMon entry address. This region is only a linker ceiling;
# it does not protect growth into Neo1 I/O or profile ROM address ranges.
MEMORY {
    RAM: start = $C100, size = $3F00, type = rw;
}

SEGMENTS {
    CODE: load = RAM, type = ro;
}
