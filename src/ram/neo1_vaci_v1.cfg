# Link VACI at its WozMon entry address and keep all future growth below the
# $D000 device page. Runtime ordinary-transfer checks reserve this full region.
MEMORY {
    RAM: start = $C100, size = $0F00, type = rw;
}

SEGMENTS {
    CODE: load = RAM, type = ro;
}
