# The utility occupies $1800-$2FFF at most; its fixed staging buffer begins at
# $3000, so linker overflow fails before code and staging memory can overlap.
MEMORY {
    RAM: start = $1800, size = $1800, type = rw;
}

SEGMENTS {
    CODE: load = RAM, type = ro;
}
